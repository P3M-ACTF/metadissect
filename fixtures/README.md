# Fixtures

| File | Source |
|------|--------|
| `minimal.exe` | Synthetic PE32 with Rich Header, `.text`, and `kernel32.dll!GetProcAddress` import (no VS_VERSIONINFO / Authenticode). Generated by `cargo run -p metadissect --example write_exec_fixtures`. |
| `minimal.elf` | Synthetic ELF64 with `NT_GNU_BUILD_ID` note (`deadbeef01020304`). Same generator. |
| `c2pa-sample.png` | Synthetic 8×8 PNG with an embedded C2PA manifest store, signed with `c2pa` crate `EphemeralSigner` (`metadissect-fixture.local`). Generated for MetaDissect Phase 3 tests — not a real camera/vendor claim. Expect `ValidationState=valid` with `signingCredential.untrusted` (no trust anchors bundled). |
| `sample.warc` | Synthetic WARC 1.0 with `warcinfo` + `response` (Target-URI, IP, payload digest, HTTP Server). `cargo run -p metadissect --example write_phase5_fixtures`. |
| `sample.msg` | Synthetic Outlook MSG (OLE/MAPI) with Subject/From/To/Cc/Message-ID/date and one attachment name. Same generator. Full MAPI set is intentionally not claimed. |
| Other samples | Optional local samples used by integration tests when present. |
